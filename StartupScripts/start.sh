!# /bin/bash


#In bash, the # edits out in green, comments you can add to your code.
#It is imperative that you # or comment out every line of code that 
#is a comment so you can add context to a problems in a section of code.
sudo apt-get update && upgrade -y
sudo apt install htop net-tools macchanger -y
sudo apt install gnome-boxes -y
sudo apt install rpi-imager -y
sudo apt install cmatrix -y
sudo chmod +x second.shs
sudo bash second.sh